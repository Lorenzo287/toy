\ Regression tests for boundary-aware lexing.

'check [ [ ] [ "lexer regression failed" error ] ifelse ] def

[1 2+] [ 1 2 + ] == check
3[1 2]cons [ 3 1 2 ] == check

10|x|$x 1+ 11 == check
10 20| a b |$a$b + 30 == check

[-1 -2.5 +3 +4.5] [ -1 -2.5 3 4.5 ] == check

"a,b,c"","split [ "a" "b" "c" ] == check
"abc"["b"!=]filter "ac" == check

( outer ( inner ) still outer )
true check

/* Block comments ignore [ ( { | : ; and
   continue until the closing delimiter. */
true check

1/* tight block comment */2 + 3 == check
1/* sign boundary */-2 + -1 == check
7 dup/* word boundary comment */+ 14 == check
[ 1 /* comment inside vector */ 2 ] [ 1 2 ] == check

"lexer solid ok" print
